{
  "name": "mlb-sun-tracker-src",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd .. && npm install",
    "build": "cd .. && npm run build",
    "start": "cd .. && npm start"
  }
}